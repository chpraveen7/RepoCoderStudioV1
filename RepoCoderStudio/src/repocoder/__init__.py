"""RepoCoder Studio — a phased, repository-aware AI coding assistant.

The public surface is intentionally small; import from submodules directly, e.g.::

    from repocoder.common.env import detect_platform
    from repocoder.common.model_loader import load_model
"""

__version__ = "0.1.0"
