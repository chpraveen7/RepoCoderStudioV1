"""Stage 6 entrypoint (skeleton on `main`). Full implementation on `stage-6-agentic`."""

from __future__ import annotations


def run_stage(cfg) -> dict:
    raise NotImplementedError(
        "Stage 6 is implemented on branch `stage-6-agentic`. Run: git checkout stage-6-agentic"
    )
