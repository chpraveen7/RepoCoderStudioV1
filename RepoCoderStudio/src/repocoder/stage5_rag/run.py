"""Stage 5 entrypoint (skeleton on `main`). Full implementation on `stage-5-rag`."""

from __future__ import annotations


def run_stage(cfg) -> dict:
    raise NotImplementedError(
        "Stage 5 is implemented on branch `stage-5-rag`. Run: git checkout stage-5-rag"
    )
