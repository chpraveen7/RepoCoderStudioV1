"""Stage 4 entrypoint (skeleton on `main`). Full implementation on `stage-4-repo-awareness`."""

from __future__ import annotations


def run_stage(cfg) -> dict:
    raise NotImplementedError(
        "Stage 4 is implemented on branch `stage-4-repo-awareness`. "
        "Run: git checkout stage-4-repo-awareness"
    )
